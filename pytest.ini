[pytest]
pythonpath = backend
asyncio_mode = strict
testpaths = tests
markers =
    unit: fast unit tests, no external dependencies
    integration: requires live database
    e2e: full pipeline, requires live database and Groq API key
    security: security and injection tests
